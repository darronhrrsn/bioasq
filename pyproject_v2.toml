[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "bioasq-local"
version = "0.0.1"

[tool.setuptools]
package-dir = {"" = "lib"}
py-modules = ["bioasq_model_v2"]